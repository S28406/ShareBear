<Window x:Class="PRO_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRO_"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Users" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />
            <ListBox x:Name="UsersListBox" DisplayMemberPath="Username" Height="350" />
        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Tools" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />
            <ListBox x:Name="ToolsListBox" DisplayMemberPath="Name" Height="350" />
        </StackPanel>
    </Grid>

</Window>
