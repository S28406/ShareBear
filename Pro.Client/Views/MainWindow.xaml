<Window x:Class="Pro.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShareBear" Height="900" Width="1440"
        Background="#0B1220"
        TextElement.FontFamily="Segoe UI"
        TextElement.FontSize="13"
        TextElement.Foreground="#E5E7EB">

    <Window.Resources>
        <SolidColorBrush x:Key="ShellBg" Color="#0B1220"/>
        <SolidColorBrush x:Key="TopBarBg" Color="#0E1627"/>
        <SolidColorBrush x:Key="PanelBg" Color="#111B2E"/>
        <SolidColorBrush x:Key="CardBg" Color="#0F1A2E"/>
        <SolidColorBrush x:Key="BorderC" Color="#24314D"/>

        <SolidColorBrush x:Key="TextPrimary" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#9CA3AF"/>

        <SolidColorBrush x:Key="Accent" Color="#F97316"/>
        <SolidColorBrush x:Key="AccentHover" Color="#EA580C"/>

        <Style TargetType="{x:Type Button}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderC}"/>
            <Setter Property="Background" Value="{StaticResource PanelBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#17233A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#0E172A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="#0B1220"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentHover}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderC}"/>
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource Accent}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True">

        <Border DockPanel.Dock="Top"
                Background="{StaticResource TopBarBg}"
                BorderBrush="{StaticResource BorderC}"
                BorderThickness="0,0,0,1"
                Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="34" Height="34" CornerRadius="10" Background="{StaticResource Accent}" Margin="0,0,10,0">
                        <TextBlock Text="🐻" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="ShareBear" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Tool sharing marketplace" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="SearchTextBox"
                             Width="520"
                             KeyDown="SearchTextBox_KeyDown"/>
                    <Button Content="Search"
                            Width="110"
                            Height="40"
                            Margin="12,0,0,0"
                            Click="Search_Click"
                            Style="{StaticResource PrimaryButton}"/>
                </StackPanel>

                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Button x:Name="AdminBtn" Content="Admin" Click="GoAdmin_Click"
                            Margin="0,0,8,0"
                            Visibility="Collapsed"/>

                    <Button x:Name="AddProductBtn" Content="List a Tool" Click="GoAddProduct_Click"
                            Margin="0,0,8,0"
                            Style="{StaticResource PrimaryButton}"
                            Visibility="Collapsed"/>

                    <Button x:Name="MyToolsBtn" Content="My Tools" Click="GoMyTools_Click"
                            Margin="0,0,8,0"
                            Visibility="Collapsed"/>

                    <Button x:Name="RegisterBtn" Content="Register" Click="GoRegister_Click"
                            Margin="0,0,8,0"
                            Style="{StaticResource PrimaryButton}"/>

                    <Button x:Name="HistoryBtn" Content="History" Click="GoHistory_Click"
                            Margin="0,0,8,0"
                            Visibility="Collapsed"/>

                    <Button x:Name="LoginBtn" Content="Sign In" Click="GoLogin_Click"
                            Margin="0,0,8,0"/>

                    <Button x:Name="LogoutBtn" Content="Log out" Click="Logout_Click"
                            Margin="0"
                            Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Bottom"
                Background="{StaticResource TopBarBg}"
                BorderBrush="{StaticResource BorderC}"
                BorderThickness="0,1,0,0"
                Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="© 2024 ShareBear. All rights reserved."
                           Foreground="{StaticResource TextSecondary}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Help Center" Margin="0,0,16,0" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock Text="Contact Info" Margin="0,0,16,0" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock Text="Resources" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Background="{StaticResource ShellBg}" Padding="20">
            <Border Background="{StaticResource TopBarBg}" CornerRadius="16" BorderBrush="{StaticResource BorderC}" BorderThickness="1">
                <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden"/>
            </Border>
        </Border>

    </DockPanel>
</Window>
